Fonctionnalités à implémenter

    Représenter un graphe par dictionnaire d'adjacence ex: {'A': ['B','C'], ...}
    bfs(graphe, depart) : parcours en largeur avec une file (Queue)
    dfs(graphe, depart) : parcours en profondeur avec une pile (Stack) ou récursion
    Détecter si un chemin existe entre deux noeuds
    Détecter si le graphe contient un cycle