Classe Stack : push(val), pop(), peek(), is_empty(), size()
Classe Queue : enqueue(val), dequeue(), front(), is_empty(), size()
Lever une erreur explicite si pop/dequeue sur structure vide
Exercice bonus : utiliser ta Stack pour vérifier si des parenthèses sont bien équilibrées ex: '(()())' → True, '(()' → False




